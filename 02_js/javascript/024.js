/*24 - Crie um script que declare um array de strings e utilize a função `join()` 
para concatenar todas as strings em uma única string, separadas por vírgulas. 
Imprima o resultado.*/

var string = ['ovos', 'queijo', 'leite'];
const stringModificada = string.join(', ');
console.log(stringModificada);